# moon-securitytxt 项目申报书

## 基本信息

- 项目名称：moon-securitytxt
- 参赛者：谭海杰
- 联系方式：已通过赛事官方报名渠道提交（公开仓库不展示个人邮箱）
- GitHub 仓库链接：https://github.com/moyunijieshi1/moon-securitytxt
- 项目方向：MoonBit 标准协议工具库／Web 安全基础设施
- 是否为移植项目：否，为原创项目

## 项目简介

moon-securitytxt 是一个使用 MoonBit 实现的 RFC 9116 `security.txt` 工具库，用于解析、校验、生成和审计网站提供的 `/.well-known/security.txt` 文件。该文件可以向安全研究人员说明漏洞报告渠道、披露策略、加密密钥、有效期限、规范地址和首选语言等信息。

项目采用纯本地设计，不包含 HTTP 请求、漏洞扫描、证书验证或 OpenPGP 密码学验证功能。调用方负责提供文件内容和获取上下文，工具库只进行确定性的格式处理与语义检查。

## 项目方向与适用场景

项目面向 MoonBit Web 工具链、站点管理程序、安全合规工具和协议处理库，适用于以下场景：

- 检查网站生成的 `security.txt` 是否符合 RFC 9116；
- 将文件内容解析为便于程序处理的结构化数据；
- 在部署前检查联系方式、过期时间和 Canonical 地址；
- 生成格式统一、结果可复现的 `security.txt`；
- 对缺少策略链接、文件过期、地址不一致等情况给出审计提示；
- 为 MoonBit 服务端程序、CLI 工具或持续集成流程提供基础能力。

## 核心功能范围

- 支持注释、空行、LF／CRLF、UTF-8 和扩展字段；
- 支持 `Contact`、`Expires`、`Canonical`、`Encryption`、`Acknowledgments`、`Policy`、`Hiring` 和 `Preferred-Languages`；
- 保留字段原始顺序，并按照出现顺序处理多个联系方式和 Canonical 地址；
- 提供结构化错误，记录处理阶段、错误类型、行号、列号和字节偏移；
- 校验必需字段、字段基数、URI、HTTPS Web 地址、RFC 3339 时间和语言标签；
- 提供过期判断、剩余有效时间和即将过期提示；
- 根据调用方提供的获取 URI 和 Content-Type 校验文件上下文；
- 提供确定性序列化和经过校验的文档生成器；
- 检测 OpenPGP cleartext signature envelope，提取正文并标记为未验证签名；
- 提供 `parse`、`validate`、`fresh`、`generate`、`audit` 和 `stats` CLI 命令；
- 设置输入大小、行数、字段数和字段长度限制，增强恶意输入下的健壮性；
- 提供跨 wasm-gc、JavaScript 和 native 目标的测试、示例及一键验证脚本。

## 原创与参考说明

本项目为原创实现，不是现有开源项目的移植，也未复制第三方库代码。协议行为主要依据 IETF RFC 9116，并使用其中的示例作为标准一致性测试数据；相关来源与版权信息已记录在 `THIRD_PARTY_NOTICES.md` 中。

- 参考标准：RFC 9116 — A File Format to Aid in Security Vulnerability Disclosure
- 标准链接：https://www.rfc-editor.org/rfc/rfc9116.html
- 项目运行时依赖：MoonBit 标准库
- 项目许可证：Apache License 2.0

项目仅提供 `security.txt` 信息处理能力。文件中出现联系方式不代表任何人获得了扫描、攻击或渗透目标系统的许可。

## 完成度与终审交付

- 当前终审版本为 `0.1.0`，已发布到 Mooncakes，模块名、GitHub 仓库和包名保持一致；
- 有效 MoonBit 源码共 5492 行，其中核心库代码 2946 行、测试代码 1811 行、CLI 与示例代码 735 行；
- 具有 102 个命名测试，并包含确定性的属性网格和截断输入健壮性用例；
- wasm、wasm-gc、JavaScript 与 native 四个目标均通过 `--deny-warn` 严格检查和测试；
- 提供五个可运行示例、完整 CLI、架构与 API 文档、RFC 9116 规范映射、限制说明和安全边界；
- 提供一键本地验证脚本与 GitHub Actions，覆盖格式、编译、测试、CLI 冒烟、示例和打包清单；
- 项目仅依赖 MoonBit 标准库，采用 Apache-2.0 许可证，第三方标准样例来源已单独声明。

项目已从开发版本整理为首个稳定版本，核心协议实现未为终审进行无意义扩写；本次整理集中于稳定版号、严格质量门禁、持续集成、可复现验证和公开材料隐私清理。
